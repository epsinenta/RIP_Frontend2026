# Локальный backend для разработки (эквивалентно http://localhost:8080).
VITE_DEV_API_PROXY=http://127.0.0.1:8080
# Оставляем пустым, чтобы запросы шли на /api и проксировались через Vite на VITE_DEV_API_PROXY.
VITE_API_ORIGIN=
VITE_MINIO_PUBLIC_BASE=http://localhost:9000/test
